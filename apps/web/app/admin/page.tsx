import { backendFetch } from "@/lib/backend-client"; import { getSessionToken } from "@/lib/session";
export default async function AdminPage(){const token=await getSessionToken(); const data=await (await backendFetch("/admin/overview",{},token??undefined)).json() as {totalUsers:number;totalVideos:number}; return <section><h1 className="mb-8 text-3xl font-semibold">Dashboard</h1><div className="grid gap-5 sm:grid-cols-2"><Metric label="Total users" value={data.totalUsers}/><Metric label="Total videos" value={data.totalVideos}/></div></section>}
function Metric({label,value}:{label:string;value:number}){return <div className="rounded-xl border border-line bg-surface p-6 shadow-vm-sm"><p className="text-sm text-muted">{label}</p><p className="mt-3 text-4xl font-semibold">{value}</p></div>}
