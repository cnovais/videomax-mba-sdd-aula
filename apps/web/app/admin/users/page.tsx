import { backendFetch } from "@/lib/backend-client"; import { getSessionToken } from "@/lib/session"; import { AdminUsersTable, type AdminUser } from "@/components/admin-users-table";
export default async function UsersPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const params=await searchParams; const query=new URLSearchParams(); for(const [key,value] of Object.entries(params)) if(value) query.set(key,value); const token=await getSessionToken(); const data=await (await backendFetch(`/admin/users?${query}`,{},token??undefined)).json() as {items:AdminUser[];page:number;pageSize:number;total:number}; return <section><h1 className="mb-8 text-3xl font-semibold">Users</h1><AdminUsersTable {...data}/></section>}
