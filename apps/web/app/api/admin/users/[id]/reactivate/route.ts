import {NextResponse} from "next/server";import {backendFetch} from "@/lib/backend-client";import {requireSessionToken} from "@/lib/session";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const t=await requireSessionToken();if(t instanceof Response)return t;const r=await backendFetch(`/admin/users/${(await params).id}/reactivate`,{method:"POST"},t);return NextResponse.json(await r.json(),{status:r.status});}
