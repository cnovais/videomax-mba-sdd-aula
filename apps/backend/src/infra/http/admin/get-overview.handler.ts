import type {Handler}from"@/infra/http/handler";import type{HttpRequest,HttpResponse}from"@/infra/http/types";import type{GetAdminOverviewUseCase}from"@/usecase/user/get-admin-overview.usecase";export class AdminOverviewHandler implements Handler{constructor(private readonly usecase:GetAdminOverviewUseCase){}async handle(req:HttpRequest):Promise<HttpResponse>{return{status:200,body:await this.usecase.execute(req.user?{actorId:req.user.id}:{})};}}
