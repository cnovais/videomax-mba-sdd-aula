# dependencies
node_modules/
/.pnp
.pnp.*

# testing
coverage/
test-results/
playwright-report/
blob-report/

# next.js
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example
!.env.test

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# scripts/init.sh runtime state (PID files, dev-server logs)
.pids/

# on-demand-generated oversized video fixture (scripts/generate-oversized-video.sh)
# — never commit this; regenerate locally whenever it's needed
video-samples/oversized*

# F03 local video storage (uploaded files + extracted thumbnails)
apps/backend/storage/
apps/backend/storage-test/
