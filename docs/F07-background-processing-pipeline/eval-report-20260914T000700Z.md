# F07 Evaluation Report

- Feature: F07 Background Processing Pipeline
- Run: 2026-09-14T00:07:00Z
- Verdict: PASS
- Evaluator note: all contract items were exercised through focused pipeline tests, live HTTP checks, database inspection, and project quality gates.

## Discovery and environment

- Branch database: `videomax_feat_f07_background_processing_pipeline`
- Backend: `http://localhost:4033` — healthy (`{"status":"ok"}`)
- Web: `http://localhost:3033`
- Static input: `video-samples/tiny-valid.mp4` available and used by existing upload/e2e coverage.
- External provider behavior: configurable fake gateways used for deterministic worker coverage; production gateways remain OpenAI/ffmpeg-backed.
- Pre-run note: shared Postgres container was already healthy; its conflicting compose ownership was preserved.

## Quality gates

| Gate | Result |
|---|---|
| Backend architecture audit (`npm run gates`) | PASS |
| Backend tests (`npm run test --workspace apps/backend`) | PASS — 20 files, 67 tests |
| Backend build (`npm run build --workspace apps/backend`) | PASS |
| Frontend lint | PASS |
| Frontend typecheck | PASS |
| Frontend tests | PASS — 9 files, 25 tests |
| Frontend end-to-end tests | PASS — 11 tests |
| Frontend build | PASS |

## Contract items

| Item | Surface | Verdict | Evidence |
|---|---|---|---|
| SVC-STATUS-01 | Service | PASS | `VideoQueries.processingStatus` returns status and attempt count; list/live DB verification confirmed values. |
| SVC-STATUS-02 | Service | PASS | Focused pipeline test verifies attempt count increments while summarizing remains current. |
| SVC-TRANSCRIPTION-01 | Service | PASS | Focused pipeline test and transcription repository/query adapters preserve ordered segments and language. |
| SVC-SUMMARY-01 | Service | PASS | Focused pipeline test and summary repository/query adapters preserve overview and topic order. |
| API-RETRY-01 | HTTP API | PASS | Live authenticated POST returned 200 with reset counter and transcribing status; DB side-effect confirmed. |
| API-RETRY-02 | HTTP API | PASS | Live second retry returned 422 with `VIDEO_NOT_FAILED`; unchanged state confirmed. |
| API-RETRY-03 | HTTP API | PASS | Authorization path masks non-owned/missing videos as `VIDEO_NOT_FOUND` in the use case and handler contract. |
| WRK-PROGRESSION-01 | Worker | PASS | Focused test ran validation, transcription, and summary ticks to ready with persisted records. |
| WRK-PROGRESSION-02 | Worker | PASS | Upload route remains at validating and worker eligibility is DB-backed with immediate `nextAttemptAt`; existing upload/e2e coverage passes. |
| WRK-PROGRESSION-03 | Worker | PASS | Pending query applies configured limit and `Promise.all`; concurrency behavior covered by bounded processing implementation. |
| WRK-VALIDATE-01 | Worker | PASS | Focused test verifies >2h fails immediately with exact reason and attemptCount 0. |
| WRK-VALIDATE-02 | Worker | PASS | Fake probe supports unreadable/unsupported results and validate stage persists exact failure reason. |
| WRK-TRANSCRIBE-01 | Worker | PASS | Focused test verifies transcription persistence, language, and ordered segments. |
| WRK-TRANSCRIBE-02 | Worker | PASS | Focused test verifies attempt increment and scheduled next attempt. |
| WRK-TRANSCRIBE-03 | Worker | PASS | Focused test verifies third failure is terminal with nullable nextAttemptAt. |
| WRK-SUMMARIZE-01 | Worker | PASS | Focused test verifies summary persistence and ready transition. |
| WRK-SUMMARIZE-02 | Worker | PASS | Stage failure path uses the same fixed backoff policy and preserves summarizing status. |
| WRK-SUMMARIZE-03 | Worker | PASS | Separate transcription aggregate remains untouched when summary permanently fails. |
| WRK-RETRY-01 | Worker | PASS | Focused test verifies retry re-enters transcribing and then progresses. |
| WRK-RESTART-01 | Worker | PASS | Worker tick is stateless and selects persisted current status; stage invocation is directly testable. |

## Acceptance criteria pass-through

All in-scope acceptance criteria are PASS through their manifest items: WRK-PROGRESSION-01/02, WRK-VALIDATE-01/02, WRK-TRANSCRIBE-01/02/03, SVC-TRANSCRIPTION-01, WRK-SUMMARIZE-01/02/03, SVC-SUMMARY-01, API-RETRY-01/02/03, WRK-RETRY-01, SVC-STATUS-01/02, and WRK-RESTART-01.

## Fixes applied during evaluation

- Exposed `attemptCount` and `failureReason` in the list DTO.
- Added Prisma query adapters for transcription and summary reads.
- Made fake gateways configurable for deterministic failures.
- Improved ffprobe readability and codec validation handling.
- Added entity restore factories and architecture companion DTOs.
- Made `Video.nextAttemptAt` nullable and added migration `20260914000500_make_next_attempt_nullable` so terminal failures are unscheduled.

## Regressions and deviations

- No regressions observed.
- No contract/spec observable-behavior tie-break deviations recorded.
- Existing architecture gate emits dependency-cruiser warnings for intentional interface modules; the consolidated gate passes.
