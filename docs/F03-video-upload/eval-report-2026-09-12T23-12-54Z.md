# Eval Report — F03 Video Upload

*Generated by evaluator. Each run produces a new timestamped file. Do not hand-edit.*

**Run:** `2026-09-12T23-12-54Z`
**Branch:** `f03`
**Status:** `pending`
**Items:** `4` PASS · `0` FAIL · `7` BLOCKED · `0` MANUAL · `0` SKIPPED (out of `11`)
**ACs:** `1` ✓ verified · `0` ✗ failed · `6` ⊘ undetermined (out of `7`)

---

## Discovery

| Decision | Layer | Source |
|---|---|---|
| Bring-up command | 2 | `CLAUDE.md` / `AGENTS.md`: "Use `./scripts/init.sh` to start the project" and "Do not run app dev servers directly." Reused the already-running `init.sh`-managed instance for branch `f03` (backend `:4081` pid `48829`, web `:3081` pid `24701`, db `videomax_f03`) instead of spawning a second, evaluator-owned instance. |
| Ephemeral DB / process isolation | 2 (override of skill default) | Same `CLAUDE.md` rule forecloses hand-starting a second backend/web pair with a distinct `DATABASE_URL`. Reused `videomax_f03` (the branch DB `init.sh` already created and migrated) in place of a fresh `eval_f03_<run-id>` DB. Logged under Soft-fails. |
| Migration command | 5 | `apps/backend/package.json` → `prisma:migrate` (`prisma migrate deploy`); schema already applied and verified live by querying `videomax_f03.users`/`videomax_f03.videos` directly. |
| Seed mechanism | 5 | `apps/backend/prisma/seed.ts` via `npx prisma db seed` (wired into `scripts/init.sh:run_seed`). Fixture user already present; verified directly. |
| Reset between items | 6 | No project-documented per-item reset mechanism found. Default applied: `DELETE FROM videos WHERE "userId" = '<fixture-user-id>'` run before each HTTP API item to honor each item's "no prior videos" given. |
| HTTP driver | 5 | Plain HTTP client (`curl`) against `http://localhost:4081`, using `Authorization: Bearer <sessionToken>` obtained from `POST /auth/login` (backend resolves sessions via bearer token — see `apps/backend/src/infra/http/middleware/auth.ts`). |
| UI / E2E driver | 7 (abort for these items only) | `CLAUDE.md` mandates loading a `playwright-cli` skill for all navigation tests and explicitly forbids raw Playwright as a substitute. No `playwright-cli` skill exists in this session's skill catalog (`.claude/skills/` has only `clean-arch`, `evaluator`, `fix-runner`, `implement-and-evaluate*`, `implement-feature`, `prd-writer`, `spec-writer`). Loaded `claude-in-chrome` as the only other browser-automation option; it reported the extension is not connected in this session. No compliant UI driver available — `UI-*` and `E2E-*` items gated `BLOCKED` at pre-flight (Step 6.3), not exercised. |

---

## Pre-run cleanup

Scanned for orphans matching the feature's marker before bring-up:
- Databases matching `eval_f03_*`: none found (`videomax`, `videomax_test`, `videomax_seed`, `videomax_f03` exist — none match the marker).
- Tmpdirs matching `evaluator-f03-*` under `$TMPDIR`: none found.
- `docs/F03-video-upload/.orchestrate.lock` (PID `24791`, confirmed dead via `kill -0`) is an artifact of the `implement-and-evaluate` orchestrator, not this skill's own marker (`evaluator-f03-*` / `eval_f03_*`). Left untouched per the marker-only cleanup rule. No live evaluator lockfile (`evaluator-f03-*/processes.lock`) was found, so the concurrent-run safety check passed.

*(No orphans removed.)*

---

## Quality gates

- ✓ `Backend architecture audit` — exit `0` — `npm run gates` (TypeScript, ESLint, dependency-cruiser, architecture check, madge, knip all clean; dependency-cruiser reported 7 pre-existing `no-orphans` warnings only, 0 errors)
- ✓ `Backend tests` — exit `0` — `npm run test --workspace apps/backend` (19 files, 62 tests passed)
- ✓ `Backend build` — exit `0` — `npm run build --workspace apps/backend`
- ✓ `Frontend lint` — exit `0` — `npm run lint --workspace apps/web`
- ✓ `Frontend typecheck` — exit `0` — `npm run typecheck --workspace apps/web`
- ✓ `Frontend unit/component tests` — exit `0` — `npm run test --workspace apps/web` (9 files, 25 tests passed)
- ✓ `Frontend end-to-end tests` — exit `0` — `npm run test:e2e --workspace apps/web` (11/11 Playwright tests passed — previously failing gate, fixed by commit `ba4b2bc` between this run and the prior `eval-report-2026-09-12T22-52-23Z.md`, which made `playwright.config.ts` read the branch's actual resolved ports instead of hardcoded `4000`/`3000`)
- ✓ `Frontend build` — exit `0` — `npm run build --workspace apps/web` (Next.js production build completed; all routes compiled)

All 8 gates passed. Proceeded to pre-flight and item execution.

---

## Pre-flight (Prerequisites)

### Runtime services
- ✓ `the backend HTTP service (apps/backend) is running, migrated, and reachable` — reachable at `http://localhost:4081`, `GET /health` returned `{"status":"ok"}`
- ✓ `the apps/web Next.js application is running and reachable by a browser, proxy able to reach backend` — reachable at `http://localhost:3081`, `GET /` returned `200`
- ✗ `a browser (or browser-automation engine) is available to drive UI and E2E items` — no compliant driver: `playwright-cli` skill absent from this session's skill catalog; `claude-in-chrome` extension not connected; `CLAUDE.md` forbids raw Playwright as a substitute for navigation tests
- ✓ `ffmpeg and ffprobe are available on PATH wherever the backend process runs` — `ffmpeg 7.1.1`, `ffprobe 7.1.1` at `/opt/homebrew/bin`

### Persistent state
- ✓ `fixture user "Existing User" <existing@example.com> / ValidPass123, not suspended, not admin` — verified via direct query against `videomax_f03.users`: `id=b72b8647-43af-48e9-8d01-304025c2f2c9`, `isAdmin=f`, `isSuspended=f`; `POST /auth/login` also succeeded and returned a `sessionToken`

### Static inputs
- ✓ `video-samples/tiny-valid.mp4` — `ffprobe`: streams `h264 (video)` + `aac (audio)`, `duration=3.0s`, size `67,818 B` (< 1 MB)
- ✓ `video-samples/audio-only.mp4` — `ffprobe`: single `aac (audio)` stream, no video stream, `duration=3.0s`, size `27,654 B`
- ✓ `video-samples/unsupported-format.txt` — `file`: `ASCII text`, size `86 B`

### Configuration
- ✓ `frontend recognizes videomax_session cookie as authenticated-visitor signal` — mechanism present in `apps/web/lib/session.ts`
- ✓ `backend session-signing key configured and stable` — `SESSION_SECRET` set in `apps/backend/.env`, read by `apps/backend/src/config/env.ts`
- ✓ `frontend's internal backend base URL configured` — `BACKEND_INTERNAL_URL=http://localhost:4081` in `apps/web/.env.local`
- ✓ `backend's local-disk storage root configured and writable` — `STORAGE_ROOT=./storage` in `apps/backend/.env`; `apps/backend/storage/{videos,thumbnails}` exist and are writable

### External dependencies
*(covered under Runtime services / ffmpeg-ffprobe above; no additional entries declared)*

---

## Coverage Manifest

| AC (verbatim from PRD) | Mark | Items |
|---|---|---|
| User can upload MP4, MOV, MKV, WEBM, or AVI files up to 2GB via drag-and-drop or file picker | ⊘ undetermined | `API-UPLOAD-01` (PASS), `UI-UPLOAD-01` (BLOCKED: no browser driver) |
| Upload progress shows filename, percentage, and bytes transferred | ⊘ undetermined | `UI-UPLOAD-02` (BLOCKED: no browser driver) |
| Files above 2GB are rejected before transfer with a clear message | ⊘ undetermined | `UI-UPLOAD-03` (BLOCKED: no browser driver) |
| Unsupported extensions are rejected before transfer with a clear message | ⊘ undetermined | `UI-UPLOAD-04` (BLOCKED: no browser driver), `API-UPLOAD-03` (PASS) |
| After a successful upload, the video appears in the library with its extracted thumbnail and status `validating` | ⊘ undetermined | `API-UPLOAD-01` (PASS), `UI-UPLOAD-01` (BLOCKED: no browser driver), `E2E-UPLOAD-01` (BLOCKED: no browser driver) |
| Thumbnail extraction failure falls back to a placeholder and does not block processing | ✓ verified | `API-UPLOAD-02` (PASS) |
| User can continue browsing the library while the upload is in progress | ⊘ undetermined | `UI-UPLOAD-05` (BLOCKED: no browser driver) |

---

## Items

### HTTP API

#### `API-UPLOAD-01` — happy path stores the file and probes its metadata

**Verdict:** PASS

**Bullets:**
- ✓ response is `201` with body `{ id, status: "validating", durationSeconds: a positive number, containerFormat: "mp4" }` | expected `201` + shape · observed `201`, `{"id":"27203127-6b5a-4571-a35f-93b25857d565","status":"validating","durationSeconds":3,"containerFormat":"mp4",...}`
- ✓ the response's `thumbnailUrl` is non-null | expected non-null · observed `"/videos/27203127-6b5a-4571-a35f-93b25857d565/thumbnail"`
- ✓ a video row now exists for this user with that id, status `validating`, and the same duration | expected row present · observed `videomax_f03.videos`: `id=27203127-6b5a-4571-a35f-93b25857d565, status=validating, durationSeconds=3`

**Evidence**

<details>
<summary>Show raw evidence</summary>

```
POST /videos (multipart: video-samples/tiny-valid.mp4) → 201
{"id":"27203127-6b5a-4571-a35f-93b25857d565","title":"tiny-valid","description":"","originalFilename":"tiny-valid.mp4","sizeBytes":67818,"durationSeconds":3,"containerFormat":"mp4","status":"validating","thumbnailUrl":"/videos/27203127-6b5a-4571-a35f-93b25857d565/thumbnail","uploadedAt":"2026-09-12T23:11:55.904Z"}

SELECT id, status, "durationSeconds" FROM videos WHERE id='27203127-6b5a-4571-a35f-93b25857d565';
 27203127-6b5a-4571-a35f-93b25857d565 | validating | 3
```

</details>

#### `API-UPLOAD-02` — thumbnail extraction failure does not block the upload

**Verdict:** PASS

**Bullets:**
- ✓ response is `201` with a non-null `durationSeconds` | expected `201` + non-null duration · observed `201`, `durationSeconds:3`
- ✓ the response's `thumbnailUrl` is null | expected null · observed `thumbnailUrl:null`
- ✓ the video row is persisted with status `validating` despite the missing thumbnail | expected row status `validating` · observed `videomax_f03.videos`: `id=3e9c3c61-1851-42ca-b808-65498c00a424, status=validating`

**Evidence**

<details>
<summary>Show raw evidence</summary>

```
POST /videos (multipart: video-samples/audio-only.mp4) → 201
{"id":"3e9c3c61-1851-42ca-b808-65498c00a424","title":"audio-only","description":"","originalFilename":"audio-only.mp4","sizeBytes":27654,"durationSeconds":3,"containerFormat":"mp4","status":"validating","thumbnailUrl":null,"uploadedAt":"2026-09-12T23:12:08.664Z"}

SELECT id, status FROM videos WHERE id='3e9c3c61-1851-42ca-b808-65498c00a424';
 3e9c3c61-1851-42ca-b808-65498c00a424 | validating
```

</details>

#### `API-UPLOAD-03` — unsupported format rejected at the API boundary

**Verdict:** PASS

**Bullets:**
- ✓ response is `422` with `code: "UNSUPPORTED_FORMAT"` | expected `422` + code · observed `422`, `{"code":"UNSUPPORTED_FORMAT","message":"Only MP4, MOV, MKV, WEBM, and AVI files are supported: \"unsupported-format.txt\""}`
- ✓ no video row is created | expected `0` rows · observed `count(*) = 0` for the fixture user immediately after the request

**Evidence**

<details>
<summary>Show raw evidence</summary>

```
POST /videos (multipart: video-samples/unsupported-format.txt) → 422
{"code":"UNSUPPORTED_FORMAT","message":"Only MP4, MOV, MKV, WEBM, and AVI files are supported: \"unsupported-format.txt\""}

SELECT count(*) FROM videos WHERE "userId"='b72b8647-43af-48e9-8d01-304025c2f2c9';
 0
```

</details>

#### `API-LIST-01` — lists only the authenticated user's videos

**Verdict:** PASS

**Bullets:**
- ✓ response is `200` with `items` including that video's id, title, status, and `thumbnailUrl` | expected `200` + shape · observed `200`, `items:[{"id":"c2c49929-3364-48e8-b074-ead0501e9b0c","title":"tiny-valid","status":"validating","thumbnailUrl":"/videos/c2c49929-3364-48e8-b074-ead0501e9b0c/thumbnail",...}]`
- ✓ `total` equals the number of videos belonging to this user | expected `total==1` (one video uploaded as given) · observed `total:1`

**Evidence**

<details>
<summary>Show raw evidence</summary>

```
given: POST /videos (multipart: video-samples/tiny-valid.mp4) → 201 id=c2c49929-3364-48e8-b074-ead0501e9b0c

GET /videos → 200
{"items":[{"id":"c2c49929-3364-48e8-b074-ead0501e9b0c","title":"tiny-valid","status":"validating","thumbnailUrl":"/videos/c2c49929-3364-48e8-b074-ead0501e9b0c/thumbnail","sizeBytes":67818,"durationSeconds":3,"uploadedAt":"2026-09-12T23:12:28.443Z"}],"page":1,"pageSize":20,"total":1}
```

</details>

### UI

#### `UI-UPLOAD-01` — drag-and-drop a valid file uploads and appears in the library

**Verdict:** BLOCKED — no browser driver: `playwright-cli` skill absent, `claude-in-chrome` not connected, raw Playwright disallowed by `CLAUDE.md`

#### `UI-UPLOAD-02` — progress card shows percentage and bytes transferred

**Verdict:** BLOCKED — no browser driver (same reason as `UI-UPLOAD-01`)

#### `UI-UPLOAD-03` — oversized file rejected before transfer

**Verdict:** BLOCKED — no browser driver (same reason as `UI-UPLOAD-01`)

#### `UI-UPLOAD-04` — unsupported extension rejected before transfer

**Verdict:** BLOCKED — no browser driver (same reason as `UI-UPLOAD-01`)

#### `UI-UPLOAD-05` — user can keep browsing the library while an upload is in progress

**Verdict:** BLOCKED — no browser driver (same reason as `UI-UPLOAD-01`)

### E2E

#### `E2E-UPLOAD-01` — an uploaded video persists and survives reload

**Verdict:** BLOCKED — no browser driver (same reason as `UI-UPLOAD-01`)

#### `E2E-UPLOAD-02` — a second upload queues behind the first and both persist

**Verdict:** BLOCKED — no browser driver (same reason as `UI-UPLOAD-01`)

---

## Overrides applied

*(none)*

## Overrides ignored

*(none)*

## Soft-fails / out-of-band

- `ephemeral DB substituted` — used the branch's existing `videomax_f03` database in place of a fresh `eval_f03_<run-id>` DB, and reused the already-running `init.sh`-managed backend/web instead of spawning a separate evaluator-owned pair, because `CLAUDE.md` requires `./scripts/init.sh` for bring-up and forbids running dev servers directly.
- `no UI/E2E driver available` — `playwright-cli` (mandated by `CLAUDE.md`) is not an installed skill in this session; `claude-in-chrome` (the only other browser-automation option) is not connected; raw Playwright is explicitly disallowed by `CLAUDE.md` as a substitute for navigation tests. The project's own Playwright E2E suite (run as the "Frontend end-to-end tests" quality gate) still ran and passed — that is the project's automated regression suite, not this skill directly driving the browser to exercise `UI-*` / `E2E-*` contract items, so it does not satisfy this prerequisite.
- `fixture user's video table not empty at pre-flight` — 19 pre-existing rows found for `existing@example.com` in `videomax_f03.videos` at the start of this run (residue from prior manual/implementation-time smoke testing and the prior eval run). Cleared via `DELETE FROM videos WHERE "userId"=...` before each HTTP API item per the contract's "no prior videos" given.
- `quality-gate regression fixed since last run` — the prior report (`eval-report-2026-09-12T22-52-23Z.md`) aborted at the "Frontend end-to-end tests" gate (port mismatch between `playwright.config.ts` hardcoded ports and the branch's resolved ports). Commit `ba4b2bc` ("fix(F03): cycle 1 …", authored between that report and this run) fixed it; all 8 gates now pass.

## Abort reason

*(not applicable — run completed Steps 1–8 without aborting; status is `pending` due to `⊘` ACs from the unavailable browser driver, not from an abort)*
